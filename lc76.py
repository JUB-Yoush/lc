#https://leetcode.com/problems/minimum-window-substring/
'''
just checking for numbers out of order, a set or hashmap could be helpful.
have two sets of two pointers?
although we count duplicates, so a hashmap would make more sense
dude I really should be working on graph and dp problems
'''
class Solution:
    def minWindow(self, s: str, t: str) -> str: